name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows D3D12
            os: windows-latest
            backend: D3D12
          - name: Ubuntu Vulkan
            os: ubuntu-latest
            backend: Vulkan
          - name: macOS Metal
            os: macos-latest
            backend: Metal

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libvulkan-dev xorg-dev

      - name: Configure
        run: cmake -S . -B build -DDY_BACKEND=${{ matrix.backend }} -DCMAKE_BUILD_TYPE=Release

      - name: Build engine library
        run: cmake --build build --config Release --target dy_engine --parallel
